[tool.poetry]
name = "snowguard-access"
version = "0.1.0"
description = "A CLI application that can be used to monitor the accsess privelages  on a snowflake acount."
authors = ["Simen Svenkrud"]
license = "Apache 2.0"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
click = "^8.0.3"
pyyaml = "^6.0.1"
regex = "^2023.10.3"
dictdiffer = "^0.9.0"
wheel = "^0.41.2"
setuptools = "^78.1.1"

[tool.poetry.group.dev.dependencies]
pytest = ">=7.2,<10.0"
pytest-cov = "^3.0.0"
pytest-mock = "^3.6.1"
black = "^24.3.0"
isort = "^5.9.3"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
